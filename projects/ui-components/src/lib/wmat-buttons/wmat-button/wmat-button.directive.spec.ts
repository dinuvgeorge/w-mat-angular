import { WmatButtonDirective } from './wmat-button.directive';

describe('WmatButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new WmatButtonDirective();
    expect(directive).toBeTruthy();
  });
});
