import { WmatTabDirective } from './wmat-tab.directive';

describe('WmatTabDirective', () => {
  it('should create an instance', () => {
    const directive = new WmatTabDirective();
    expect(directive).toBeTruthy();
  });
});
